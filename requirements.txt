# ============================================================
# HeightAI 智能身高识别系统 - Python 依赖
# ============================================================

# Intel RealSense SDK (可选，用于深度相机)
pyrealsense2>=2.50.0

# MediaPipe 姿态估计 (核心依赖)
mediapipe>=0.10.0

# 图像处理 (核心依赖)
opencv-contrib-python>=4.8.0

# 科学计算 (核心依赖)
numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# Web 框架 (核心依赖)
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.8.0
python-engineio>=4.5.0

# WebSocket 依赖
eventlet>=0.33.0
gevent>=23.0.0
gevent-websocket>=0.10.0

# 配置管理
pyyaml>=6.0
python-dotenv>=1.0.0

# 日志系统
loguru>=0.7.0

# 数据处理
pandas>=2.0.0

# 图像处理
Pillow>=10.0.0

# 报告生成
reportlab>=4.0.0

# 3D 可视化 (可选)
open3d>=0.17.0
matplotlib>=3.7.0

# 类型检查
typing-extensions>=4.5.0

# RTMPose (可选，需要 CUDA)
# pip install onnxruntime-gpu
onnxruntime>=1.16.0

# 简单 WebSocket (备用)
simple-websocket>=1.0.0
websocket-client>=1.6.0
